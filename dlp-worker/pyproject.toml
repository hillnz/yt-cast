[project]
name = "dlp-worker"
version = "0.1.0"
description = "Queue consumer that builds podcast feeds and caches audio in R2"
requires-python = ">=3.12"
dependencies = ["httpx"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "workers-py",
    "workers-runtime-sdk",
]
