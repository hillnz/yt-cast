[project]
name = "drive-proxy"
version = "0.1.0"
description = "Google Drive to Cloudflare R2 proxy worker"
requires-python = ">=3.12"
dependencies = ["fastapi", "httpx"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "workers-py",
    "workers-runtime-sdk",
]
